jar {
    archiveBaseName = 'spaceclipse-run-editor'
    archiveVersion = project.version.toString()
}

dependencies {
    implementation project(':run:client')
    implementation project(':editor')
}