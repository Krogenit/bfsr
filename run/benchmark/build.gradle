jar {
    archiveBaseName = 'spaceclipse-run-benchmark'
    archiveVersion = project.version.toString()
}

dependencies {
    implementation project(':run:client')
    implementation project(':benchmark')
}