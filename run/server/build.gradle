jar {
    archiveBaseName = 'spaceclipse-run-server'
    archiveVersion = project.version.toString()
}

dependencies {
    implementation project(':engine:common')
    implementation project(':game-logic:server')
}