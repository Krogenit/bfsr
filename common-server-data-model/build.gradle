jar {
    archiveBaseName = 'spaceclipse-common-server-data-model'
    archiveVersion = project.version.toString()
}

dependencies {
    api project(':game-logic:common')

    api 'org.springframework.boot:spring-boot-starter-data-mongodb-reactive:4.0.4'
}
